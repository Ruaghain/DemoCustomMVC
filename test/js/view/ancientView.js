
(function (require) {
	var fs = require('../../../src/js/view/ancientView');
	describe('b', function(require) {
	  it('should equal fixture contents', function() {
		expect(true).toEqual(true);
	  });

	});
}(require));

